# Model contract — generated from Sales.SemanticModel by scripts/gen-model-contract.py. Do not hand-edit; regenerate.
model: Sales
status: approved
generated: 2026-09-02
version: 1
tables:
  - name: "Date"
    type: dimension
    hidden: false
    columns:
      - { name: "Date", dataType: dateTime }
      - { name: "Fiscal Year", dataType: string }
      - { name: "Fiscal Month", dataType: string }
  - name: "Product"
    type: dimension
    hidden: false
    columns:
      - { name: "Product", dataType: string }
      - { name: "Category", dataType: string }
  - name: "Sales"
    type: fact
    hidden: true
  - name: "Store"
    type: dimension
    hidden: false
    columns:
      - { name: "Store", dataType: string }
      - { name: "Region", dataType: string }
  - name: "_Measures"
    type: measures
    hidden: false
    measures:
      - { name: "Net Sales (AUD)", format: "#,0", description: "Sales net of GST and returns, in AUD." }
      - { name: "Baskets #", format: "#,0", description: "Number of distinct receipts (baskets)." }
      - { name: "Avg Basket (AUD)", format: "#,0.00", description: "Net sales per basket, in AUD." }
      - { name: "Net Sales PY (AUD)", format: "#,0", description: "Net sales for the same period in the prior fiscal year, in AUD." }
      - { name: "Net Sales YoY %", format: "0.0%", description: "Year-over-year growth in net sales." }
roles:
  - "RLS - Region"
