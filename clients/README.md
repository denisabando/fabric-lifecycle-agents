# clients/

`_template/` is committed and is what `/new-engagement <code>` copies.
`acme-demo/` is a committed sample engagement used by the evals and for demos.
Every other folder here is git-ignored: real engagements live in the client's repo or in a
private per-client repo that mounts this one as a submodule.
