Original design notes and workflow diagram from the first draft (v0.1). File names and rule ids
have since changed (`fsm-*` prefix dropped, `MS-*` → `MOD-*`, contract replaced by pinned commit);
the architecture they describe is unchanged.
